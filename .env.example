# Environment Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# Application
NODE_ENV=development
PORT=5000

# Database (PostgreSQL)
# For Render: Use the Internal Database URL from your Render PostgreSQL dashboard
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://user:password@localhost:5432/trendz

# Session Security
# Generate a strong random secret: openssl rand -base64 32
SESSION_SECRET=your-secret-key-change-in-production

# Object Storage (Google Cloud Storage via Replit)
# These values come from setting up Object Storage in Replit
# If deploying to Render, you'll need to set up your own GCS bucket
DEFAULT_OBJECT_STORAGE_BUCKET_ID=your-bucket-id
PRIVATE_OBJECT_DIR=gs://your-bucket/private
PUBLIC_OBJECT_SEARCH_PATHS=gs://your-bucket/public

# Optional: If you need additional configuration
# CORS_ORIGIN=https://yourdomain.com
# MAX_FILE_SIZE=52428800
